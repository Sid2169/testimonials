# Public build-time config for the admin dashboard's dev server only.
# To point the header "View website" link at a different website, copy this
# file to apps/admin/.env and change the value (production uses Caddy env args).
VITE_WEBSITE_URL=http://localhost:5173