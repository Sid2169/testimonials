# Public build-time config for the website's dev server only.
# To point the header "Admin" link at a different admin origin, copy this file
# to apps/website/.env and change the value (production uses compose Caddy args).
VITE_ADMIN_URL=http://localhost:5174